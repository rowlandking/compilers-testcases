function : void foo(int & x) {}
function : void main() {
	int x = sizeof(1.3); //not a type and not addressable
	//What is a case of being a type but not being addressable?
	int y = sizeof(float);
 	sizeof(x) = 2; //invalid
	foo(sizeof(x)); //invalid
}
